#include <stdio.h>

int main()
{

    // int number = 12;  =========%d
    // float number = 10.123; =========%f
    // double number = 10.12345678911111; =========%lf

    // int x = 10;
    // int y = 5;
    // '+'
    // '-'
    // '*'
    // '/'

    // int sum = x + y;
    // printf("Sum = %d", x+y);

    // // Minus
    // float minus = x - y;
    // printf("Minus = %.4f", minus);

    // Multiplication
    // int x = 3;
    // int y = 5;

    // int multi = x * y;
    // printf("Result = %d", 3*5);

    // Division

    // float a = 3;
    // float y = 5;

    // float division = a/y;
    // printf("%f", division);

    // int number;
    // & = and
    // & = address of

    // printf("Input your Number = \n");
    // scanf("%d", &number);
    // printf("Stored Number= %d", number);

    // char full form character

    // char bloodGroup = "+";
    // printf("%c", bloodGroup);

    // String

    // char string[] = "Today is saturday";
    // printf("%s", string);

    // int num1 , num2;

    // char inputTxt[] = "Input your Numbers" ;
    // printf("%s \n", inputTxt);

    // printf("First Number = ");
    // scanf("%d", &num1);

    // printf("\nSecond Number = ");
    // scanf("%d", &num2);

    // printf("Sum = %d", num1 + num2);

    // int x,y;
    // scanf("%d %d", &x ,&y);
    // printf("%d", x+y);

    // char Nuzud[24];
    // return 0;

    // Take user input of length of a square and make a program to calculate its area.
    // Input 2 Output 4
    // Input 6 Output 36
    // Input 9 Output 81

    // Take user 2 inputs of length and breath of a rectangle and make a program to calculate its area.
    // Input 2 3        Output 6
    // Input 8 9        Output 72
    // Input 1920 1080  Output 2073600

    //Given a Number N corresponding to time in hour. Print time in Weeks, days and hours, preceded by its respective message "Weeks", "Days", "Hours".

    // Note: consider the whole week has 7 days and 24 hours day.

    // Input
    // Only one line containing a number N (0 ≤ N ≤ 5000).

    // Output
    // Print the output, like the following examples.
    // Input        Output
    // 400          Weeks 2
    //              Days 2
    //              Hours 16

    // 700          Weeks 4
    //              Days 1
    //              Hours 4

    // int x;
    // scanf("%d", &x);

    // int td = x / 24;
    // int weeks = td / 7;
    // int days = td % 7;
    // int hours = x % 24;

    // printf("Weeks %d \nDays %d \nHours %d", weeks, days, hours);




    // Given Number in Seconds is to be converted to the given type of conversion.
    // Example: 
    // Number = 3600 Type= H then Output will be 1 hour
    // Input            Output
    // 8000  M          133 Minutes
    // 8000  H          2 Hours
    // 99000 D          1 Day



    // int seconds;
    // char type;
    // scanf("%d  %c", &seconds, &type);

    // if(type == 'M'){
    //     printf("%d Minutes",seconds/60);
    // }
    // else if(type == 'H'){
    //     printf("%d Hours",seconds/3600);
    // }
    // else{
    //     printf("%d Days",seconds/(3600*24));
    // }


    int chips, biscuit, rice;

    // if price of chips is less than 20 the print we buy chips else the print we buy biscuit
    
    printf("Give Prices");
    scanf("%d", &chips);

    if(chips < 20){
        printf("We buy chips");
    }
    else{
        printf("We buy biscuit");
    }




// Operators (Symbols kowa jay)

// Arithmetic operators (+, -, *, /, %)

// Assignment operators

// Comparison operators

// Logical operators

// Bitwise operators














    return 0;
}